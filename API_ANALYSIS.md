# Анализ API dreviz-shop.ru

## Результаты тестирования (Шаг 1)

### Структура ответа API

API endpoint: `https://dreviz-shop.ru/api/search`

**Структура ответа:**
```json
{
  "searchHistory": [],
  "taps": [],
  "brands": [],
  "sections": [],
  "products": {
    "items": [],
    "meta": {}
  }
}
```

### Наблюдения

1. ✅ **API доступен** - статус 200 OK, нет ошибок CORS
2. ❌ **Пустой результат** - `products.items` возвращает пустой массив
3. ✅ **Данные есть на странице** - в HTML таблице видны товары с артикулами, ценами, наличием
4. 🔍 **Возможные причины пустого API:**
   - Требуется авторизация (cookies/session)
   - Требуются специальные заголовки
   - API работает только для авторизованных пользователей
   - Параметры запроса требуют дополнительные данные

### Вывод

API доступен, но возвращает пустые данные без авторизации. Данные товаров присутствуют на странице в HTML.

**Примечание:** У нас есть учетные данные для авторизации, но авторизация в SPA приложении сложна. Для начала используем парсинг HTML.

### Решение

**Рекомендуемый подход:** Парсинг HTML через headless browser (puppeteer/playwright)

**Причины:**
- Данные доступны на странице
- Не требует авторизации
- Можно извлекать все нужные поля (артикул, название, наличие, цена)
- Более надёжный подход для публичных данных

**Альтернатива (для будущего):** 
- Использовать API с авторизацией (если нужно)
- Учетные данные сохранены в `AUTH_CREDENTIALS.md` (не в Git)

### Следующий шаг

Переходим к реализации парсинга HTML через headless browser в Supabase Edge Function.
