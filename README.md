# ArtecoBase - База знаний производства

Платформа для хранения и управления базой знаний с информацией о прайс-листах на материалы, фурнитуре, бланках заказа и всем, что связано с производством.

## Возможности

- 📦 **Категоризация файлов**: Организация по категориям (материалы, фурнитура, бланки заказа, производство)
- 🔍 **Поиск и фильтрация**: Быстрый поиск по названию, описанию и тегам
- 📤 **Загрузка файлов**: Простая загрузка файлов с метаданными (описание, теги, категория)
- 🔗 **Обмен файлами**: Генерация ссылок для доступа к файлам
- 📥 **Скачивание**: Скачивание файлов одним кликом
- 🎨 **Современный UI**: Красивый и удобный интерфейс на основе Tailwind CSS

## Технологии

- React 19
- TypeScript
- Tailwind CSS
- Vite
- Lucide React (иконки)
- date-fns (форматирование дат)

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev-сервер:
```bash
npm run dev
```

3. Откройте браузер по адресу, указанному в консоли (обычно http://localhost:5173)

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist`.

## Структура проекта

```
src/
  ├── components/      # React компоненты
  │   ├── Header.tsx          # Шапка с поиском и кнопкой загрузки
  │   ├── Sidebar.tsx         # Боковая панель с категориями
  │   ├── FileCard.tsx        # Карточка файла
  │   ├── FileGrid.tsx        # Сетка файлов
  │   ├── UploadModal.tsx     # Модальное окно загрузки
  │   └── ShareModal.tsx      # Модальное окно обмена
  ├── hooks/           # Пользовательские хуки
  │   └── useFiles.ts         # Хук для управления файлами
  ├── types/           # TypeScript типы
  │   └── index.ts            # Типы и интерфейсы
  ├── data/            # Данные
  │   └── categories.ts       # Определения категорий
  ├── App.tsx          # Главный компонент
  └── main.tsx         # Точка входа
```

## Примечания

В текущей версии файлы хранятся в localStorage браузера. Для продакшен-версии рекомендуется:

- Интеграция с backend API (например, Express.js или Supabase)
- Хранение файлов на сервере или в облачном хранилище (AWS S3, Cloudinary и т.д.)
- Реализация аутентификации пользователей
- Настройка безопасного доступа к файлам через share-ссылки
- Добавление версионирования файлов
- Реализация комментариев и аннотаций

## Лицензия

MIT
