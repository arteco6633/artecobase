# ArtecoBase - База знаний производства

Платформа для хранения и управления базой знаний с информацией о прайс-листах на материалы, фурнитуре, бланках заказа и всем, что связано с производством.

## Возможности

- 📊 **Создание таблиц**: Создавайте и редактируйте таблицы с прайс-листами, справочниками и другими структурированными данными в реальном времени
- 📝 **Создание документов**: Создавайте текстовые документы для инструкций, заметок и другой информации
- 📦 **Категоризация**: Организация по категориям (материалы, фурнитура, бланки заказа, производство)
- 🔍 **Поиск и фильтрация**: Быстрый поиск по названию, описанию и тегам
- ✏️ **Редактирование в реальном времени**: Редактируйте таблицы и документы прямо в браузере
- 🔗 **Обмен документами**: Генерация ссылок для доступа к документам
- 🎨 **Современный UI**: Красивый и удобный интерфейс на основе Tailwind CSS

## Технологии

- React 19
- TypeScript
- Tailwind CSS
- Vite
- Lucide React (иконки)
- date-fns (форматирование дат)

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev-сервер:
```bash
npm run dev
```

3. Откройте браузер по адресу, указанному в консоли (обычно http://localhost:5173)

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist`.

## Деплой

Проект настроен для автоматического деплоя на Vercel через GitHub:

1. При каждом push в ветку `main` происходит автоматический деплой
2. Vercel автоматически определяет настройки для Vite проекта
3. Деплой происходит за несколько секунд

## Структура проекта

```
src/
  ├── components/      # React компоненты
  │   ├── Header.tsx          # Шапка с поиском и кнопкой создания
  │   ├── Sidebar.tsx         # Боковая панель с категориями
  │   ├── DocumentCard.tsx    # Карточка документа/таблицы
  │   ├── DocumentGrid.tsx    # Сетка документов
  │   ├── CreateModal.tsx     # Модальное окно выбора типа создания
  │   ├── TableEditor.tsx     # Редактор таблиц
  │   ├── DocumentEditor.tsx  # Редактор документов
  │   ├── TableViewer.tsx     # Просмотр таблиц
  │   ├── DocumentViewer.tsx  # Просмотр документов
  │   └── ShareModal.tsx      # Модальное окно обмена
  ├── hooks/           # Пользовательские хуки
  │   └── useDocuments.ts     # Хук для управления документами
  ├── types/           # TypeScript типы
  │   └── index.ts            # Типы и интерфейсы
  ├── data/            # Данные
  │   └── categories.ts       # Определения категорий
  ├── App.tsx          # Главный компонент
  └── main.tsx         # Точка входа
```

## Примечания

В текущей версии данные хранятся в localStorage браузера. Для продакшен-версии рекомендуется:

- Интеграция с backend API (например, Express.js или Supabase)
- Хранение данных на сервере или в базе данных
- Реализация аутентификации пользователей
- Настройка безопасного доступа к документам через share-ссылки
- Добавление версионирования документов
- Реализация комментариев и аннотаций

## Лицензия

MIT
